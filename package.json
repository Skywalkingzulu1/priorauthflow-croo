{
  "name": "prior-auth-flow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/agent/orchestrator.ts",
    "build": "tsup src/agent/orchestrator.ts --format esm --out-dir dist",
    "test": "vitest run src/tests",
    "pages": "npx serve public -l 4173"
  }
}
